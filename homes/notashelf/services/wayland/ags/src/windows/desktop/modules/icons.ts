import { Widget } from "../../../imports";
const { Box } = Widget;

const DesktopIcons = () =>
	Box({
		className: "desktopIcons",
		vertical: true,
		hpack: "start",
		vpack: "start",
		children: [],
	});

export default DesktopIcons;
